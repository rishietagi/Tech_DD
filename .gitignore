# env / secrets
.env
.env.*
!.env.example

# python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
*.egg-info/

# db
*.db
*.sqlite3

# node / next
node_modules/
.next/
out/
dist/
coverage/
frontend/.env*.local
*.tsbuildinfo

# playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# conda
.conda/

# editor / os
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log

# Confidential source material — never commit.
# The KPMG scope deck is client-confidential. Its *content* lives in
# docs/reference/KPMG_SOW_LANGUAGE.md (transcribed house voice), which is what the
# engine actually reads; the PDF itself stays local.
*.pdf
docs/reference/*.pdf

# Test fixture for the checklist's shared-drive scanner. Local scaffolding only — the
# repo should not carry fake documents that could later be mistaken for real ones.
# See docs/phases/PHASE4_PLAN.md.
shared-drive/
